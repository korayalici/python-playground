# Python Playground

Minimal Python project to practice a clean repo layout (`src/`), virtual environments, and testing.

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install -e .
